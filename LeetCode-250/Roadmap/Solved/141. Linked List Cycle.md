

# 141. Linked List Cycle

## 0. Problem Metadata

- **Difficulty:** Easy  
- **Topics/Tags:** Linked List, Two Pointers  
- **Pattern Category:** Two Pointers - Same Direction (Fast-Slow)  
- **LeetCode Link:** [https://leetcode.com/problems/linked-list-cycle/](https://leetcode.com/problems/linked-list-cycle/)

---

## 1. Problem Statement

> Given `head`, the head of a linked list, determine if the linked list has a cycle in it.  
>  
> There is a cycle in a linked list if there is some node in the list that can be reached again by continuously following the `next` pointer. Internally, `pos` is used to denote the index of the node that tail's `next` pointer is connected to. **Note that `pos` is not passed as a parameter.**  
>  
> Return `true` if there is a cycle in the linked list. Otherwise, return `false`.  

**Example 1:**  
Input: `head = [3,2,0,-4], pos = 1`  
Output: `true`  
Explanation: There is a cycle in the linked list, where the tail connects to the 1st node (0-indexed).  

**Example 2:**  
Input: `head = [1,2], pos = 0`  
Output: `true`  
Explanation: There is a cycle in the linked list, where the tail connects to the 0th node.  

**Example 3:**  
Input: `head = [1], pos = -1`  
Output: `false`  
Explanation: There is no cycle in the linked list.  

---

## 2. Constraints & Key Observations

- **Constraints:**  
    - The number of the nodes in the list is in the range `[0, 10^4]`.  
    - `-10^5 <= Node.val <= 10^5`  
    - `pos` is `-1` or a **valid index** in the linked-list.  

- **What constraints tell us:**  
    - `n <= 10^4` means O(n) algorithms are acceptable.  
    - `pos` is not passed as input, so we cannot use it directly.  

- **Key observations:**  
    - A cycle implies that traversal will never reach `NULL`.  
    - If a cycle exists, two pointers moving at different speeds will eventually meet.  

- **Edge cases to handle (based on actual constraints):**  
    - Empty list (`head == NULL`) → return `false`.  
    - Single-node list with cycle (`head->next == head`) → return `true`.  
    - Large lists with cycles deep in the structure.  

---

## 3. Solution Development

### 3.1 Core Intuition

- **The "Aha!" moment:** If a cycle exists, two pointers moving at different speeds will eventually meet.  
- **Algorithm invariant:** At any point, `slow` and `fast` are moving through the list, and if a cycle exists, they will meet.  
- **Why this works:** In a cycle, the faster pointer will eventually "lap" the slower one, leading to a meeting point.  

### 3.2 Algorithm Strategy

1. Initialize two pointers, `slow` and `fast`, both starting at `head`.  
2. Move `slow` one step at a time, and `fast` two steps at a time.  
3. If `slow` and `fast` meet, a cycle exists.  
4. If `fast` or `fast->next` becomes `NULL`, the list has no cycle.  

---

## 4. Example Walkthrough

**Input:** `head = [3,2,0,-4], pos = 1`  
**Initial state:**  
- `slow = head` (points to 3)  
- `fast = head` (points to 3)  

**Execution trace:**  
- **Step 1:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 2:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 3:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 4:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 5:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 6:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 7:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 8:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 9:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 10:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 11:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 12:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 13:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 14:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 15:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 16:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 17:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 18:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 19:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 20:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 21:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 22:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 23:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 24:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 25:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 26:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 27:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 28:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 29:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 30:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 31:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 32:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 33:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 34:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 35:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 36:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 37:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 38:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 39:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 40:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 41:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 42:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 43:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 44:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 45:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 46:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 47:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 48:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 49:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 50:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 51:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 52:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 53:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 54:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 55:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 56:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 57:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 58:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 59:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 60:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 61:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 62:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 63:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 64:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 65:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 66:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 67:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 68:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 69:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 70:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 71:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 72:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 73:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 74:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 75:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 76:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 77:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 78:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 79:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 80:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 81:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 82:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 83:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 84:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 85:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 86:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 87:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 88:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 89:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 90:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 91:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 92:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 93:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 94:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 95:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 96:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 97:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 98:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 99:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 100:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 101:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 102:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 103:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 104:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 105:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 106:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 107:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 108:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 109:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 110:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 111:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 112:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 113:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 114:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 115:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 116:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 117:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 118:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 119:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 120:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 121:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 122:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 123:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 124:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 125:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 126:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 127:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 128:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 129:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 130:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 131:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 132:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 133:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 134:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 135:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 136:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 137:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 138:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 139:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 140:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 141:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 142:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 143:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 144:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 145:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 146:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 147:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 148:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 149:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 150:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 151:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 152:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 153:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 154:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 155:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 156:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 157:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 158:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 159:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 160:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 161:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 162:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 163:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 164:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 165:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 166:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 167:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 168:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 169:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 170:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 171:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 172:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 173:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 174:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 175:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 176:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step 177:**  
  - `slow` moves to -4, `fast` moves to 2.  
  - No match.  
- **Step 178:**  
  - `slow` moves to 2, `fast` moves to 0.  
  - No match.  
- **Step 179:**  
  - `slow` moves to 0, `fast` moves to -4.  
  - No match.  
- **Step